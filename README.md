# 绿洲鱼宠 - 观赏鱼宠物管理应用

一个基于 React + Vite + React Router 构建的观赏鱼宠物管理应用，支持部署到 GitHub Pages。

## 功能特性

- 🐟 宠物注册：支持鱼类、龟类等分类
- 📝 宠物列表：展示所有注册的宠物
- 📋 详情页面：查看宠物详细信息
- ✏️ 编辑功能：修改宠物信息
- 💀 死亡标记：标记宠物死亡状态
- 🌊 放生功能：标记宠物放生状态
- 🗑️ 删除功能：删除宠物记录
- 📱 PWA 支持：可安装到手机桌面

## 技术栈

- **React 18** - UI 框架
- **Vite** - 构建工具
- **React Router** - 路由管理（使用 HashRouter，适配 GitHub Pages）
- **Tailwind CSS** - 样式框架
- **shadcn/ui** - UI 组件库
- **localStorage** - 数据存储

## 开发

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build

# 预览构建结果
pnpm preview
```

## 部署到 GitHub Pages

项目将部署到 `https://oasis-cloud.github.io/pets/`

### 部署步骤

1. 确保已安装依赖：
   ```bash
   pnpm install
   ```

2. 构建并部署到 GitHub Pages：
   ```bash
   pnpm deploy
   ```

   这个命令会：
   - 构建项目到 `dist` 目录
   - 将 `dist` 目录的内容推送到 `https://github.com/oasis-cloud/oasis-cloud.github.io.git` 仓库的 `main` 分支
   - 文件会被部署到仓库的 `pets` 目录

3. 访问网站：
   - 主页面：`https://oasis-cloud.github.io/pets/`
   - 列表页：`https://oasis-cloud.github.io/pets/#/list`

## 项目结构

```
├── app/
│   ├── components/      # 组件
│   ├── lib/            # 工具函数
│   ├── pages/          # 页面组件
│   ├── App.jsx         # 主应用组件
│   ├── main.jsx        # 入口文件
│   └── tailwind.css    # 样式文件
├── public/             # 静态资源
├── dist/               # 构建输出
└── vite.config.js      # Vite 配置
```

## 路由说明

由于使用 HashRouter，所有路由都会以 `#` 开头：
- `https://oasis-cloud.github.io/pets/#/` - 注册页面
- `https://oasis-cloud.github.io/pets/#/list` - 列表页面
- `https://oasis-cloud.github.io/pets/#/fish/:id` - 详情页面
- `https://oasis-cloud.github.io/pets/#/edit/:id` - 编辑页面

## 数据存储

应用使用浏览器的 `localStorage` 存储数据，数据保存在本地浏览器中。

## License

ISC

